import React from 'react'

import c from './Header.module.css'
const Header = () => {
  return (
    <header className={`${c.header} flex flexCenter blackBg whiteFont `}>
        
    </header>
  )
}

export default Header